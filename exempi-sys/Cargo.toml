[package]
name = "exempi-sys"
version = "2.3.0"
authors = ["Hubert Figuière <hub@figuiere.net>"]
license = "BSD"
build = "build.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
pkg-config = "0.3"
