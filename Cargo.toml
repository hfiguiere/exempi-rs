[package]
name = "exempi"
version = "2.3.0"
authors = ["Hubert Figuière <hub@figuiere.net>"]
license = "BSD"
keywords = ["metadata", "tags", "xmp"]

[dependencies]
libc = "0.2"
exempi-sys = { path = "exempi-sys", version = "2.3.0" }
